# Oxlint Configuration

Shared [Oxlint](https://oxc.rs/docs/guide/usage/linter) rules for the CardWise monorepo.

Replaces `@cardwise/eslint-config` (ADR-048). Lint runs from the repository root for maximum speed — see Turborepo Oxc guide.
