{
  "name": "@cardwise/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types for CardWise",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo '@cardwise/types: build deferred to M-005+'",
    "lint": "echo '@cardwise/types: lint via root oxlint'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
