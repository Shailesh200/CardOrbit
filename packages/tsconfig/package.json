{
  "name": "@cardwise/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript configuration for CardWise",
  "files": ["base.json", "react.json", "node.json"],
  "scripts": {
    "build": "echo '@cardwise/tsconfig: no build step'",
    "lint": "echo '@cardwise/tsconfig: lint via root oxlint'",
    "typecheck": "echo '@cardwise/tsconfig: typecheck skipped (config package)'"
  }
}
